# Suricata threshold.config — FP rate-limiting and suppression
# Format: suppress gen_id <gid>, sig_id <sid>[, track <by_src|by_dst>, ip <ip>]
# Format: threshold gen_id <gid>, sig_id <sid>, type <type>, track <by>, count <n>, seconds <s>

# Add suppressions here as FP patterns emerge during tuning
